{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": [".next/**", "dist/**"]
        },
        "build:desktop": {
            "cache": false,
            "outputs": [".next/**", "dist/**"]
        }
    }
}
